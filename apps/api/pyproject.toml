[project]
name = "airton-api"
version = "0.1.0"
description = "Airton's streaming Workers AI chat API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
